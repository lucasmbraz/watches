name: watches
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  # Freezed
  freezed_annotation: ^0.12.0

  # SVG support
  flutter_svg: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  # Needed to fix build_runner bug (https://github.com/simolus3/moor/issues/553)
  analyzer: 0.39.14

  # Freezed
  freezed: ^0.12.1

flutter:

  uses-material-design: true

  assets:
    - assets/